# Node / TypeScript
host/node_modules/
host/dist/
host/dist-test/
host/*.log
npm-debug.log*

# Godot
.godot/
*.import

# Secrets / credentials (never commit)
.env
.env.*
*.pem
*.key

# Editor / OS
.DS_Store
.vscode/
.idea/

# transient / undeletable-via-bridge scratch (delete manually)
_to_delete/
