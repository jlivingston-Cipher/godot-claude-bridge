{
  "name": "godot-claude-bridge-host",
  "version": "0.4.3",
  "description": "MCP host exposing Godot to Claude across four planes (CLI, live editor, LSP+DAP, runtime) with elicitation-gated destructive tools, progress-streamed jobs, captured console output, and MCP resources.",
  "type": "module",
  "bin": {
    "godot-claude-bridge": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
